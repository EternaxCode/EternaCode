/*─────────────────────────────────────────────
  Starfield – Global Tunable Constants
  ※ 각 항목 뒤 주석에 “효과”와 추천 **min ~ max** 범위를 적어 두었습니다.
     숫자를 바꿔가며 실험해 보세요!
─────────────────────────────────────────────*/
export const C = {
  /* ──────────── 별(Points) ──────────── */
  STAR: {
    count          : 600,                 // [100 ~ 2 000]   전체 별 개수
    range          : 800,                 // [200 ~ 2 000]   별 배치 반경(±) — 값이 크면 공간이 넓어짐
    baseSize       : 8,                   // [2 ~ 20]        별 기본 크기(px)
    twinkleRange   : 0.3,                 // [0 ~ 3]         깜빡임 시 크기 변동 범위
    twinkleSpeed   : 0.02,                // [0 ~ 0.10]      값이 클수록 빠르게 깜빡임
    flashBoostInit : 3,                   // [0 ~ 3]         Pulse 충돌 시 최초 밝기 배수
    flashDecay     : 0.94,                // [0.50 ~ 0.98]   1프레임당 밝기 감쇠율 (값↑ = 천천히 사라짐)
    flashBlinkFreq : 25,                  // [5 ~ 50] Hz   번쩍 깜빡임 빈도
    popSize        : 26,    // ⭐ 팝 스프라이트 초기 크기(px)
    popDecay       : 0.5,  // ⭐ 매 프레임 scale·opacity 곱
  },

  /* ──────────── 연결선(Line) ──────────── */
  LINE: {
    maxConnectDist : 120,                 // [20 ~ 300]      두 별 거리 ≤ 이 값이면 선을 그림
    opacity        : 0.3,                 // [0 ~ 1]         기본 선 투명도
  },

  /* ──────────── Pulse(빛 알갱이) ───────── */
  PULSE: {
    maxCount    : 1_200,                  // [100 ~ 5 000]   동시에 존재 가능한 Pulse 수
    baseSpeed   : 1.6,                    // [0.2 ~ 5]       평균 이동 속도
    speedVariance: 0.8,                   // [0 ~ 3]         개별 Pulse 속도 무작위 편차
    burstCount  : 1,                      // [1 ~ 10]        한 번에 같은 선에 생성되는 Pulse 개수
    spawnProb   : 0.0004,                 // [0 ~ 0.01]      ‘선 1개·프레임’ 당 Burst 발생 확률
  },

  /* ──────────── Trail(선 잔광) ─────────── */
  TRAIL: {
    minLife   : 240,                      // [60 ~ 600]      잔상 최소 지속 프레임
    maxLife   : 600,                      // [60 ~ 1 200]    잔상 최대 지속 프레임
    maxActive : 120,                      // [0 ~ 500]       동시에 화면에 남을 수 있는 잔상 선 수
    fadeStep  : 0.3,                      // [0.05 ~ 1]      프레임당 밝기 감소량 (값↓ = 오래 남음)
  },

  /* ───────── Tail(Pulse 꼬리) ────────────
     len = 0 으로 두면 꼬리 기능을 완전히 끌 수 있습니다.          */
  TAIL: {
    len       : 30,                       // [0 ~ 50]        꼬리 길이(버텍스 수). 0 = off
    pointSize : 0.5,                      // [0.1 ~ 4]       꼬리 점 크기(px)
    opacity   : 0.2,                      // [0 ~ 1]         꼬리 전체 투명도
  },
} as const;
